#include <iostream>
#include <cmath>

int main()
{
    std::cout << std::floor(9.4) << std::endl;
    std::cout << std::ceil(9.4) << std::endl;
    // similarly we have std::abs(), std::exp(), std::pow(), std::log(), std::log10(), std::sqrt(), std::round(),
    // some trignometric functions like std::sin(), std::cos(), std::tan(), std::asin(), std::acos(), std::atan() etc.
    
}
