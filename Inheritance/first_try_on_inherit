// Topic : - Inheritance
// files associated : player.h, player.cpp, person.h, person.cpp

/*
1 Inheritance is a mechanism where a new class (subclass) acquires the properties and methods of an existing class (superclass).

*/

#include <iostream>
#include "person.h"
#include "player.h"
using namespace std;

int main()
{
    // Person person;
    // cout << person << endl;

    Player player;
    string game {"Badminton"};
    Player player1(game);   
    cout << player << endl;
    cout << player1 << endl;
}       







