int inc_mult(int a, int b){
    return ((a++) * (++b)); // to multiply a first then increment it while b is incrementing first then multiplying we know this all things from prefix and postfic incrementation
    // return ((++a) * (++b));// to increment then multiply
}