int max(int a, int b);
int min(int a, int b);
int sum(int a, int b);

// This header file contains declaration to the functions defnitions can be present in any file in the project folder ad linker is going to find it and stitch it to our main file which we are going to compile
