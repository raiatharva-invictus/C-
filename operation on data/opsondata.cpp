#include <iostream>

int main()
{
    ///Basic operations on data///
    // add, sub,mult,div,modulus
    //operator precedence of mult, div, modulus > add, sub.

    // int x = 25;
    // int a = x/10;
    // int c = x - a * 10; // understanding modulus operator

    // int b = x%10;

    // std::cout << c << " " << b << std::endl;

    /// prefix and postix increment and decrement ///

    // // postfix increment and decrement
    // int a = 5;

    // std::cout << a++ << std::endl;
    // std::cout << a << std::endl;

    // a = 5; //resetting value

    // std::cout << a-- << std::endl;
    // std::cout << a << std::endl;


    // // prefix increment and decrement

    // a = 5;

    // std::cout << ++a << std::endl;
    // std::cout << a << std::endl;

    // a = 5; //resetting value

    // std::cout << --a << std::endl;
    // std::cout << a << std::endl;



    /// Compound assignment operators ///
    // +=, -=, *=, /=, %=

    // int a {90};
    // a /= 3;
    // std::cout << a << std::endl;


    /// Relational operators (compare stuffs) ///
    // <, >, <=, >=, ==, !=.
    // always use them within parantheses to avoid compiler errors

    // int a {10};
    // int b {20};


    /// Logical Operators ///

    // && (and), || (or), ! (not) // in paratheses is what we call them
    // we can also use them with relational operators which we require most of the time.



    return 0;
}